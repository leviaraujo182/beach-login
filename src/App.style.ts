import { makeStyles } from '@material-ui/core'

export const styles = makeStyles({
    contentleft: {
        width: '60%',
        height: '100vh'
    },
    
    contentright: {
        width: '40%'
    },

    contentmain: {
        display: 'flex',
    }
})